# Crypto Sentiment

See the [root README](../README.md) for full documentation.

## Quick Local Start

```bash
cd infra && docker-compose up -d
cd backend/crypto-backend && ./gradlew bootRun   # Terminal 1
cd frontend && npm install && npm start          # Terminal 2
```
